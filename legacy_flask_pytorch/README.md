# Legacy Flask/PyTorch app

These files are kept only for reference from the original project. The Vercel-ready app is the root Next.js project.

The old `.pth` model file is intentionally not included here to keep the deployment package light.
